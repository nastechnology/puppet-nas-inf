
node 'nhs-yrbk-0.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-1.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-2.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-3.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-4.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-5.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-6.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-7.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-8.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-9.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-10.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-11.nas.local' inherits 'winbasenode' {
  package { 'office2007nooutlook' :
    ensure  => installed,
  }

  package { 'photoshop7' :
    ensure  => installed,
  }
}

node 'nhs-yrbk-12.nas.local' {

}



